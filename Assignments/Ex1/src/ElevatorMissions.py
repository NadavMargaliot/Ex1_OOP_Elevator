import Building

class ElevatorMissions:

   def __init__(self, numOfElevators):
       elevatorNum = []
       for i in range(0 , numOfElevators):
           elevatorNum.append([])
       # upCalls = []
       # for i in range(0 , numOfElevators):
       #     upCalls.append([])
       # downCalls = []
       # for i in range(0,numOfElevators):
       #     downCalls.append([])
       #

   #def addCallForElevator(self, call, elevatorIndex):










