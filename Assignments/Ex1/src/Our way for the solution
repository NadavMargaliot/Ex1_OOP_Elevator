

calls:
Each row in the Calls list represent a call.
We are getting a lot of information


if the src floor is lower than the dest floor so the state is UP (1)
if the src floor is higher than the dest floor so the state is DOWN (-1)
every floor that the elevator gets to we uploading the elevator current floor


algo
we have a array of all the calls. each index is a call.
we have a array of all the elevators. each index is a elevator.
we have a array where we keeping all the calls for each elevator(elevatorMission).

we have a array with the size of the calls. the array will start all false.
each call that sent to a elevator will become true in the array

if a call match a elevator, so we adding the call to the elevatorMission in the elevator place

allocate:
